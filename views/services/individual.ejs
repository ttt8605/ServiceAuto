<head>
    <link rel="stylesheet" href="/stylesheets/serviceInvidual.css">
</head>

<% layout('/layouts/boilerplate') %>
  
<section class="container">
    <div class="image-side">
        <div class="carousel">
          <div class="carousel-container">
            <% service.images.forEach((img, i) => { %>
              <div class="carousel-slide <%= i === 0 ? 'active' : ''%>">
                <div class="image-carousel">
                    <img src="<%= img.url %>" alt="Image <%= i + 1 %>">
                </div>
               
              </div>
            <% }) %>
          </div>
    </div>
      
      <!-- Navigation Arrows -->
       <div class="ArrowDiv">
      <button class="prev">&larr;</button>
      <button class="next">&rarr;</button>
    </div>
    </div>
    </div>


    <div class="TextSide">
     <h3 class="PostName"><%=service.name %></h3>
     <p class="PostDescription"><%-service.description %></p>
     <form action="/#MakeAnAppoinment">
      <button class="ProgrameazateButton">Programează-te aici<p class="ArrowButton">&rarr;</p></button>
     </form>
    </div>




  </section>

<script src="/javascripts/carousel.js"></script>